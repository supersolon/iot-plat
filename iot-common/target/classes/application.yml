server:
  port: 9090

spring:
  application:
    name: iot-common
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml  # 必须修改成yaml
        group: DEFAULT_GROUP  # 缺省即可
        prefix: iot-usercenter # 缺省即可
  user:
    name: zhangsan-application
    age: 18